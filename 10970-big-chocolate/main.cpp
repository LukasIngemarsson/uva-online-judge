#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(false);

    int M, N;
    while (cin >> M >> N) {
        cout << M * N - 1 << '\n';
    }

    return 0;
}
